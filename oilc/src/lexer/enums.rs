#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TimeUnit {
    Ns,
    Us,
    Ms,
    S,
    M,
    H,
    D,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Keyword {
    Rule,
    Predicate,
    Set,
    Fact,
    Use,
    Import,
    From,
    Source,
    Match,
    Correlate,
    Graph,
    Around,
    Where,
    Within,
    Let,
    Score,
    Emit,
    Respond,
    Verify,
    Require,
    With,
    On,
    By,
    Then,
    And,
    Or,
    Not,
    In,
    StartsWith,
    EndsWith,
    Contains,
    Matches,
    Under,
    True,
    False,
    Null,
    Alert,
    Isolate,
    Revoke,
    Snapshot,
    OpenCase,
    Challenge,
    Block,
    Quarantine,
    RequireMfa,
    Notify,
    Throttle,
    Expires,
    Critical,
    High,
    Medium,
    Low,
    Informational,
    If,
    Else,
}

#[derive(Debug, Clone, PartialEq)]
pub enum TokenKind {
    StrLit(String),
    IntLit(i64),
    FloatLit(f64),
    DurationLit { value: u64, unit: TimeUnit },
    RegexLit(String),
    Ident(String),
    Kw(Keyword),
    Arrow,
    FatArrow,
    Range,
    Assign,
    Eq,
    Ne,
    Lt,
    Gt,
    Le,
    Ge,
    Plus,
    Minus,
    Star,
    Slash,
    Comma,
    Colon,
    Semicolon,
    At,
    Dot,
    LBrace,
    RBrace,
    LParen,
    RParen,
    LBracket,
    RBracket,
    Newline,
    Eof,
}
